server {  
    listen 80;
    server_name mansur.pro;

    root    /var/www/mansur.pro;
    index   index.html;

    # location / {
    #     # kill cache
    #     add_header Last-Modified $date_gmt;
    #     add_header Cache-Control 'no-store, no-cache';
    #     if_modified_since off;
    #     expires off;
    #     etag off;
    # }
}